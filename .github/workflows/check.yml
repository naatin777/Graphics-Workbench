name: Check

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-pr-description:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: "22.23.1"

      - name: Require PR verification evidence
        run: node .github/scripts/validate-pr-description.mjs
        env:
          PR_BODY: ${{ github.event.pull_request.body || '' }}

  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: "22.23.1"
          package-manager-cache: false

      - run: npm install -g npm@12.0.1
        shell: bash

      - id: npm_cache
        name: Locate npm cache
        run: echo "dir=$(npm config get cache)" >> "$GITHUB_OUTPUT"
        shell: bash

      - uses: actions/cache@v6
        with:
          path: ${{ steps.npm_cache.outputs.dir }}
          key: npm-${{ runner.os }}-${{ runner.arch }}-node-22.23.1-npm-12.0.1-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-${{ runner.arch }}-node-22.23.1-npm-12.0.1-

      - run: node --version && npm --version
        shell: bash

      - run: npm ci
        shell: bash

      - run: npm run check:all
        shell: bash
